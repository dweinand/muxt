dependencies:
  pre:
    - go get github.com/constabulary/gb/...
  override:
    - make dependencies

test:
  override:
    - make test
  post:
    # - GOOS=darwin GOARCH=amd64 gb build
    - GOOS=linux GOARCH=amd64 gb build

general:
  artifacts:
    - bin
